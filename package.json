{
  "name": "@coveops/facet-slider-dynamic-range",
  "private": false,
  "version": "1.6.0",
  "description": "facet-slider-dynamic-range",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coveops build FacetSliderDynamicRange --template typescript",
    "watch": "coveops build FacetSliderDynamicRange --template typescript --watch",
    "serve": "coveops serve",
    "precompile": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@coveops/turbo-core": "^1.1.1",
    "coveo-search-ui": "^2.9373.8"
  },
  "devDependencies": {
    "@coveops/cli": "^0.11.0"
  }
}
